{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Bot",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/cmd/bot/",
      "cwd": "${workspaceFolder}"
    }
  ]
}
